# Строка 37 (была)
print(f"Usage: {sys.argv[0]} <width> <height>")
# Исправленная (если действительно нужно двойные кавычки, но лучше одинарные)
print(f'Usage: {sys.argv[0]} <width> <height>')

# Строка 205 (была)
# Если двойные кавычки не нужны для специальных символов, замените на одинарные
# Например, если было: text = "Some text"
# Исправьте на: text = 'Some text'
